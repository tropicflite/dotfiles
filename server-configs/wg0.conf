[Interface]
# Key for server
# Bouncing = 7
# NetShield = 2
# Moderate NAT = off
# NAT-PMP (Port Forwarding) = off
# VPN Accelerator = on
PrivateKey = REDACTED
Address = 10.2.0.2/32
# Table = off prevents wg-quick from touching the routing table or DNS.
# Without this, wg0 bounces kill the default route and take down all internet.
Table = off
# wg0-up-extra.sh sets iptables rules for Pi-hole and Tailscale exit node forwarding.
PostUp = /home/matt/dotfiles/scripts/server/wg0-up-extra.sh

[Peer]
# CA#639 — replace PublicKey and Endpoint when switching servers
PublicKey = REDACTED
AllowedIPs = 0.0.0.0/0
Endpoint = 154.47.17.129:51820
PersistentKeepalive = 25
