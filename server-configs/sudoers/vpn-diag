# Backstop for vpn-diskcheck.sh's cron route-failure diagnostics (fired live
# 2026-07-02): wg state snapshot + append to the diag log.
matt ALL=(ALL) NOPASSWD: /usr/bin/wg show wg0, /usr/bin/tee -a /var/log/vpn-route-diag.log
