# Desktop-specific config (WSL2)
# Tailscale (runs on Windows, not in WSL2)
alias tsstatus='echo "Tailscale: check Windows system tray"'


# Desktop-specific locale
[ -f /etc/default/locale ] && source /etc/default/locale
cd ~
