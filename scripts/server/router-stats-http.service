[Unit]
Description=Router stats HTTP server for Homepage widget
After=network.target

[Service]
Type=simple
ExecStart=python3 -m http.server 9797 --directory /home/matt/router-stats
User=matt
Restart=on-failure

[Install]
WantedBy=multi-user.target
